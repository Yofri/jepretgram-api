import {allUsers, findUser} from './user'
import {allPosts, findPost} from './post'
import {allComments, findComment} from './comment'

export {
  allUsers, findUser,
  allPosts, findPost,
  allComments, findComment
}