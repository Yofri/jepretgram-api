import {login, createUser, updateUser, removeUser, toggleFollow} from './user'
import {createPost, updatePost, removePost, toggleLike} from './post'
import {createComment, updateComment, removeComment} from './comment'

export {
  login, createUser, updateUser, removeUser, toggleFollow,
  createPost, updatePost, removePost, toggleLike,
  createComment, updateComment, removeComment
}